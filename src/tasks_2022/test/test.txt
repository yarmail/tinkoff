Это тестовое задание.
Дается перед задачами на программирование
Буквой (V) обозначаю возможные ответы
Гарантий на правильность нет
(в скобках пишу каменты и непонятки)



1. Выберите верные утверждения
- Если 2 объекта равны по equals, то их hashCode() должен совпадать  (V)
- Если 2 объекта не равны по equals, то их hashCode() может совпадать  (V)
- Если 2 объекта не равны по equals, то их hashCode() должен не совпадать

---

2. Что делает код:
    Thread thread = new Thread(new Runnable() {
        @Override
        public void run() {
            System.out.println("Hello Word");
        }
    });
- Код выводит на консоль Hello Word
- Код создает новый объект Thread   (V)
- Код создает новый поток и выполняет метод run()

---

3. Если полю класса не задан модификатор доступа,
то будет применен:
- package-private  (V)
- private
- public

---

4. Выберите самый строгий модификатор доступа:
- public
- package-private (V)
- protected

---

5. Какой класс/интерфейс в java представляет ассоциативный массив?
- Set
- ArrayList
- Map  (V)
- Hash

---

6. Правильно ли объявлен метод
void func() {...}
void func(int a) {...}
- да (V)
- нет

---

7. Что означает ключевое слово void
- Возвращается пустое пространство памяти, чтобы разработчики могли его использовать
- Метод, объявленный с возвращаемым типом void не возвращает значение (V)
- void не поддерживается в Java

---

8. Что изображено в фрагменте кода
void func() {...}
void func(int a) {...}

- перегрузка метода  (V)
- переопределение метода
- модификация метода

---

9. Что произойдет, если добавить 11 элементов в список list

List list = new ArrayList(10);
- список автоматически расширится и все 11 элементов уместятся  (V)
- размер списка останется неизменным и уместятся только первые 10 элементов
- размер списка останется неизменным и уместятся случайные 10 элементов
- будет выброшен ArrayIndexOutOfBoundsException

---

10. Какую реализацию List предпочтительнее использовать для удаления
из него элементов по условию
- CopyOnWriteArrayList
- ArrayList
- LinkedList (V)
(не понял, что имели ввиду составители вопроса)

---

11. Есть ли ошибка в блоке кода?
try {
//...
} catch (IOException | NullPointerException e) {
//...
}
- нет, все в порядке
- ошибка компилляции, нельзя обрабатывать 2 ошибки в одном блоке catch
- ошибка компилляции, проверяемое и непроверяемое исключение  (V)
обрабатываются в одном блоке

---

12. Произойдет ли ошибка при попытке выполнить код:
    int i1 = Integer.MAX_VALUE;
    int i2 = Integer.MAX_VALUE;
    System.out.println(i1 + i2);
- Ошибки нет  (V)  //-2
- Ошибка компилляции
- Ошибка во время выполнения

---

13. Может ли конструктор быть private
- да (V)
- нет

---

14. Аргументы методов в java передаются по значению или по ссылке
- всегда по значению (V)
- всегда по ссылке
- примитивы по значению, объекты по ссылке

---

15. В конструкции try-catch-finally блок finally выполняется
- всегда   (V)
- только если не сработал ни один catch
- если в catch произошла ошибка

---

16. Что будет выведено после выполнения кода:
    int k = 0;
    for (int i = 0; i < 10; i++) {
        k = k++;
    }
    System.out.println(k);
- 10
- 9
- 0   (V)
- RuntimeException

---

17. Что будет выведено после выпонения кода
    List<String> numbers = new ArrayList<>(Arrays.asList("first", "second", "third"));
    for (String number: numbers) {
        if ("third".equals(number)) {
            numbers.add("fourth");
        }
    }
    System.out.println(numbers.size());

---
- 3
- 4
- ничего
- упадет с ошибкой   (V) (как я понимаю, иммутабельная коллекция)





